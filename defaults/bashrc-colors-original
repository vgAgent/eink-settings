# Original color-related lines from Debian's default ~/.bashrc
# Restore these to reset to system defaults

# In the dircolors block — grep aliases were commented out:
#    #alias grep='grep --color=auto'
#    #alias fgrep='fgrep --color=auto'
#    #alias egrep='egrep --color=auto'

# GCC colors were commented out:
#    #export GCC_COLORS='error=01;31:warning=01;35:note=01;36:caret=01;32:locus=01:quote=01'

# PS1 (original Debian default):
#    PS1='${debian_chroot:+($debian_chroot)}\[\033[01;32m\]\u@\h\[\033[00m\]:\[\033[01;34m\]\w\[\033[00m\]\$ '

# These were NOT set at all (system defaults apply when absent):
#    GREP_COLORS       — unset
#    LESS_TERMCAP_*    — unset
#    diff alias        — unset
